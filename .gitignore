# Secret credentials
.env
*.key
*.pem
*.secret

# API keys and tokens
*_credentials.json
service_account.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.Python
pip-log.txt

# Virtual environments
.venv/
venv/
env/
ENV/

# Raw dataset — too large for GitHub (104MB)
dataset.csv

# Large intermediate pipeline files — too large for GitHub
data/cleaned_violations.csv   # 116MB — intermediate, not needed by dashboard
data/osm_enriched.csv         # 168MB — intermediate, not needed by dashboard

# OSM network cache — can be large
cache/

# The 5 small dashboard CSVs ARE committed (hotspots, forecasts, impact_scores,
# patrol_schedule, queueing_results) — do NOT add data/ to this file

# Jupyter checkpoints
.ipynb_checkpoints/
*.ipynb

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

cache/
dataset.csv
